language: bash

before_script:
    - mkdir -p /usr/share
    - curl -L "http://downloads.sourceforge.net/shunit2/shunit2-2.0.3.tgz" | sudo tar zx -C /usr/share/shunit2/

script:
    - bash shunit2/equality_test.sh
